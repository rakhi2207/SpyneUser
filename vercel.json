vercel.json
        {
          "version": 2,
            "builds": [
              {
                "src": "./index.ts",   // server.ts is in root
                "use": "@vercel/node"
              }
            ],
            "routes": [
              {
                "src": "/.*",
                "dest": "index.ts"
              }
            ]
          }
